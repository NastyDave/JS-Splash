import React, {Component} from 'react'
import { Text } from 'react-native'

export default class LoginScreen extends Component{

    render(){
        return(
            <Text style={{ textAlign: 'center', marginTop:200, fontFamily:'GOTHIC'}}>Login Screen</Text>
        )
    }
}